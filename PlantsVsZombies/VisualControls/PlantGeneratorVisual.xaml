<Canvas x:Class="PlantsVsZombies.VisualControls.PlantGeneratorVisual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PlantsVsZombies.VisualControls"
             xmlns:models="clr-namespace:PlantsVsZombies.Models"
             xmlns:plant="clr-namespace:PlantsVsZombies.Models.Plant"
             mc:Ignorable="d"
             Width="300" Height="300"
             d:DataContext="{d:DesignInstance Type=plant:PlantGenerator, IsDesignTimeCreatable=False}" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- Body (static) -->
    <Image x:Name="BodyImage"
           Canvas.Top="145"
           Canvas.Left="0"
           Source="{Binding BodyImageSource}"/>

    <!-- Head (animated) -->
    <Image x:Name="HeadImage"
           Canvas.Top="0"
           Canvas.Left="10"
           RenderTransformOrigin="0,0.5"
           Source="{Binding HeadImageSource}">
        <Image.RenderTransform>
            <TransformGroup>
                <TranslateTransform x:Name="HeadTranslate"/>
                <RotateTransform x:Name="HeadRotate"/>
            </TransformGroup>
        </Image.RenderTransform>
        <Image.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <!-- Gentle vertical bobbing animation -->
                        <DoubleAnimation
                            Storyboard.TargetName="HeadTranslate"
                            Storyboard.TargetProperty="Y"
                            From="0"
                            To="-5"
                            Duration="0:0:1"
                            AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <SineEase EasingMode="EaseInOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        
                        <!-- Slight rotation/sway animation -->
                        <DoubleAnimation
                            Storyboard.TargetName="HeadRotate"
                            Storyboard.TargetProperty="Angle"
                            From="-2"
                            To="2"
                            Duration="0:0:1.33"
                            AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <SineEase EasingMode="EaseInOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Image.Triggers>
    </Image>
</Canvas>
