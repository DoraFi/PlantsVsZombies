<Canvas x:Class="PlantsVsZombies.VisualControls.ZombieGirlVisual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PlantsVsZombies.VisualControls"
             xmlns:models="clr-namespace:PlantsVsZombies.Models"
             xmlns:zombie="clr-namespace:PlantsVsZombies.Models.Zombie"
             mc:Ignorable="d"
             Width="300" Height="300"
             Canvas.Left="{Binding X}"
             Canvas.Top="{Binding Y}"
             d:DataContext="{d:DesignInstance Type=zombie:ZombieGirl, IsDesignTimeCreatable=False}" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Image x:Name="ShieldImage"
           RenderTransformOrigin="0.5,1"
           Source="{Binding ZombieImageSource}">
        <Image.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="ShieldScale"/>
                <RotateTransform x:Name="Rotate"/>
            </TransformGroup>
        </Image.RenderTransform>
        
        <Image.Style>
            <Style TargetType="{x:Type Image}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding State}" Value="{x:Static zombie:ZombieState.Dead}">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <!-- Rotate 90 degrees and stay there -->
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].(RotateTransform.Angle)"
                                        From="0"
                                        To="90"
                                        Duration="0:0:0.5"
                                        FillBehavior="HoldEnd">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseIn"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    
                                    <!-- Fade out -->
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="Opacity"
                                        From="1"
                                        To="0"
                                        BeginTime="0:0:0.3"
                                        Duration="0:0:0.4"
                                        FillBehavior="HoldEnd">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    
                                    <!-- Drop down slightly -->
                                    <DoubleAnimation
                                        Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)"
                                        From="1.0"
                                        To="0.7"
                                        Duration="0:0:0.5"
                                        FillBehavior="HoldEnd">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseIn"/>
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Image.Style>
        
        <Image.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard Name="PulseAnimation">
                    <Storyboard RepeatBehavior="Forever">
                        <!-- Gentle pulse animation -->
                        <DoubleAnimation
                            Storyboard.TargetName="ShieldScale"
                            Storyboard.TargetProperty="ScaleX"
                            From="1.0"
                            To="1.02"
                            Duration="0:0:2"
                            AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <SineEase EasingMode="EaseInOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation
                            Storyboard.TargetName="ShieldScale"
                            Storyboard.TargetProperty="ScaleY"
                            From="1.0"
                            To="1.02"
                            Duration="0:0:2"
                            AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <SineEase EasingMode="EaseInOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Image.Triggers>
    </Image>
</Canvas>
