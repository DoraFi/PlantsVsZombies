<Canvas x:Class="PlantsVsZombies.VisualControls.ZombieBoyVisual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PlantsVsZombies.VisualControls"
             xmlns:zombie="clr-namespace:PlantsVsZombies.Models.Zombie"
             mc:Ignorable="d"
              Width="300" Height="300"
             d:DataContext="{d:DesignInstance Type=zombie:ZombieBoy, IsDesignTimeCreatable=False}" 
             Canvas.Left="{Binding X}"
             Canvas.Top="{Binding Y}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Image x:Name="ShieldImage"
           RenderTransformOrigin="0.5,0.5"
           Source="{Binding ZombieImageSource}">
        <Image.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="ShieldScale"/>
            </TransformGroup>
        </Image.RenderTransform>
        <Image.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <!-- Gentle pulse animation -->
                        <DoubleAnimation
                            Storyboard.TargetName="ShieldScale"
                            Storyboard.TargetProperty="ScaleX"
                            From="1.0"
                            To="1.02"
                            Duration="0:0:2"
                            AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <SineEase EasingMode="EaseInOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                        <DoubleAnimation
                            Storyboard.TargetName="ShieldScale"
                            Storyboard.TargetProperty="ScaleY"
                            From="1.0"
                            To="1.02"
                            Duration="0:0:2"
                            AutoReverse="True">
                            <DoubleAnimation.EasingFunction>
                                <SineEase EasingMode="EaseInOut"/>
                            </DoubleAnimation.EasingFunction>
                        </DoubleAnimation>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Image.Triggers>
    </Image>
</Canvas>
